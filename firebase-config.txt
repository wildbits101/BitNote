// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyB0-XQtV1UOtpWi8BGjT5KJ5JW0RxWiMo8",
  authDomain: "wildmd-app.firebaseapp.com",
  projectId: "wildmd-app",
  storageBucket: "wildmd-app.firebasestorage.app",
  messagingSenderId: "607964557474",
  appId: "1:607964557474:web:1887345864a945012467fb"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);


rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}